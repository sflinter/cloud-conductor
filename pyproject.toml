# Copyright (c) 2026 Steve Flinter. MIT License.
[project]
name = "cloud-conductor"
version = "0.5.0"
description = "Lightweight CLI orchestrator for GPU cloud workloads (RunPod, Vast.ai)"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "runpod",
]

[project.scripts]
conductor = "conductor.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/conductor"]

[dependency-groups]
dev = [
    "pytest",
    "httpx",
    "shtab",
]
